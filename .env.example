# Copy this file to .env and fill in the values.
# .env is git-ignored — NEVER commit real credentials.

# --- ServiceNow PDI ---
SERVICENOW_INSTANCE_URL=
SERVICENOW_USERNAME=
SERVICENOW_PASSWORD=
# close_code used when resolving on a "respond" decision (must be valid on your instance;
# list them: /api/now/table/sys_choice?sysparm_query=name=incident^element=close_code)
SERVICENOW_CLOSE_CODE="Solution provided"

# --- Google Gemini (free key from https://aistudio.google.com) ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest

# --- Service config ---
PORT=8000
# Shared secret the Business Rule sends as the X-Webhook-Secret header.
# Leave blank on both sides to disable the check.
WEBHOOK_SHARED_SECRET=
# Set to "off" to compute the decision and log it WITHOUT writing back to ServiceNow.
SERVICENOW_WRITEBACK=on
