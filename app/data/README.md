# Vendored data

`kb_articles.json` is copied verbatim from the task asset pack
(`AI_Engineering_Task0_Assets/kb_articles.json`). It is the ONLY knowledge source the
Gemini prompt is allowed to use. The service loads it at runtime, so it lives in the repo
for reproducibility (NFR4). Do not edit.
