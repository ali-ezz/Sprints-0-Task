# Vendored test data

Copied verbatim from the task asset pack:
- `test_incidents.json` — the 3 graded tickets and their expected decisions (FR6).
- `payload_contract.json` — the exact webhook payload shape.
